package game.control;
//==============================================================================
/* Shape
 * This enum is used to keep track of the shape of each slot on the board.
 * It defines the possible shapes that can be in a slot.
 */
//==============================================================================
public enum Shape {
    X,
    O,
    EMPTY
}
